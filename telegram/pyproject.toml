[project]
name = "seed-telegram"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "telethon>=1.37",
    "cryptg>=0.4",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]
