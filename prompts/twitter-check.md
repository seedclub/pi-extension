# Check Twitter/X Authentication

Verify that you're logged into Twitter/X and can access the API.

Use `twitter_check` to verify credentials. If authentication fails, instruct the user to:
1. Open x.com in Safari, Chrome, or Firefox
2. Log in to their Twitter/X account
3. Return here and retry

The extension reads cookies from your browser automatically — no API keys needed.
